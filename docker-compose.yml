#generate Nexus repository manager with docker-compose.yml
version: '3.8'
services:
  nexus:
    image: sonatype/nexus3:latest
    container_name: nexus
    ports:
      - "8081:8081"
    volumes:
      - "./nexus-data:/nexus-data"
    restart: unless-stopped


volumes:
    nexus-data: